document.addEventListener('DOMContentLoaded',()=>{if(window.lucide)lucide.createIcons();const menuBtn=document.querySelector('.menu-button');const mobileMenu=document.querySelector('.mobile-menu');menuBtn?.addEventListener('click',()=>mobileMenu?.classList.toggle('open'));document.querySelectorAll('.mobile-menu a').forEach(a=>a.addEventListener('click',()=>mobileMenu?.classList.remove('open')));const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target)}})},{threshold:.14});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));const hero=document.querySelector('.film-stage');const heroImg=document.querySelector('.hero-media img');if(hero&&heroImg){hero.addEventListener('mousemove',e=>{const r=hero.getBoundingClientRect();const x=(e.clientX-r.left)/r.width-.5;const y=(e.clientY-r.top)/r.height-.5;heroImg.style.transform=`scale(1.04) translate(${x*-12}px,${y*-10}px)`});hero.addEventListener('mouseleave',()=>heroImg.style.transform='scale(1) translate(0,0)')}const speed=document.querySelector('.speed-panel strong');const battery=document.querySelector('.dashboard-grid>div:nth-child(1) strong');const range=document.querySelector('.dashboard-grid>div:nth-child(2) strong');let t=0;setInterval(()=>{t+=.14;if(speed)speed.textContent=Math.round(116+Math.sin(t)*13);if(battery)battery.textContent=`${Math.round(82-Math.sin(t*.45)*2)}%`;if(range)range.textContent=`${Math.round(174-Math.sin(t*.35)*5)} km`},120);const form=document.querySelector('.lead-form');form?.addEventListener('submit',e=>{e.preventDefault();const msg=form.querySelector('.form-message');if(msg)msg.textContent='Solicitação registrada. A equipe VoltRide entrará em contato.';form.reset()});let lastY=window.scrollY;const header=document.querySelector('.site-header');window.addEventListener('scroll',()=>{const y=window.scrollY;if(header){header.style.transform=y>lastY&&y>180?'translateY(-110%)':'translateY(0)';header.style.transition='transform .35s ease'}lastY=y});const cursor=document.createElement('div');cursor.style.cssText='position:fixed;width:220px;height:220px;border-radius:50%;pointer-events:none;z-index:-1;background:radial-gradient(circle,rgba(114,255,79,.12),transparent 68%);transform:translate(-50%,-50%);transition:opacity .25s;opacity:0';document.body.appendChild(cursor);window.addEventListener('mousemove',e=>{cursor.style.left=e.clientX+'px';cursor.style.top=e.clientY+'px';cursor.style.opacity='1'});window.addEventListener('mouseleave',()=>cursor.style.opacity='0')});